version: "3.9"

services:
  cloud-ide:
    build: .
    ports:
      - "5000:5000"
    environment:
      FLASK_SECRET_KEY: cloud-ide-compose-secret
      ENABLE_DOCKER_EXECUTION: "false"
      STORAGE_BACKEND: local
    volumes:
      - ./backend/data:/app/backend/data
